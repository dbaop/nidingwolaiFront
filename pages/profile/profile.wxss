/* profile.wxss */
.container {
  background-color: #f5f5f5;
  min-height: 100vh;
  padding: 20rpx;
}

/* 用户信息卡片 */
.user-card {
  background-color: #ffffff;
  border-radius: 16rpx;
  padding: 30rpx;
  margin-bottom: 20rpx;
  display: flex;
  align-items: flex-start;
  box-shadow: 0 4rpx 12rpx rgba(0, 0, 0, 0.05);
  min-height: 200rpx;
}

.avatar-container {
  position: relative;
  margin-right: 30rpx;
  flex-shrink: 0;
}

.avatar {
  width: 160rpx;
  height: 160rpx;
  border-radius: 50%;
  object-fit: cover;
  border: 4rpx solid #f86b5f;
  background-color: #f5f5f5;
  display: block;
}

/* 未登录状态时的头像样式 */
.user-card.not-logged-in .avatar {
  border-color: #cccccc;
  opacity: 0.7;
}

.user-card.not-logged-in .avatar-overlay {
  background-color: rgba(0, 0, 0, 0.2);
}

.user-card.logged-in .avatar {
  border-color: #f86b5f;
  opacity: 1;
}

.avatar-overlay {
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background-color: rgba(0, 0, 0, 0.3);
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
}

.avatar-icon {
  font-size: 60rpx;
  color: #ffffff;
}

.user-info {
  flex: 1;
  display: flex;
  flex-direction: column;
  justify-content: flex-start;
  padding-left: 0;
  min-height: 160rpx;
}

.user-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-bottom: 20rpx;
  min-height: 50rpx;
}

.nickname {
  font-size: 36rpx;
  font-weight: 600;
  color: #333333;
  line-height: 50rpx;
}

.user-stats {
  display: flex;
  height: 100rpx;
  align-items: center;
  justify-content: flex-start;
  padding: 0;
  gap: 30rpx;
}

.stat-item {
  flex: 0;
  text-align: left;
  display: flex;
  flex-direction: column;
  align-items: flex-start;
  justify-content: center;
  min-width: 80rpx;
}

.stat-number {
  display: block;
  font-size: 28rpx;
  font-weight: 500;
  color: #f86b5f;
  margin-bottom: 4rpx;
  line-height: 1.2;
}

.stat-label {
  display: block;
  font-size: 22rpx;
  color: #666666;
  line-height: 1.2;
}

/* 登录提示区域 */
.login-prompt {
  flex: 1;
  display: flex;
  flex-direction: column;
  justify-content: center;
  align-items: flex-start;
  height: 100rpx;
  padding: 0;
}

.prompt-text {
  font-size: 26rpx;
  color: #999999;
  text-align: left;
  line-height: 1.4;
  margin-bottom: 14rpx;
  padding-left: 0;
}

.login-btn-prompt {
  background: linear-gradient(135deg, #FF6B8B 0%, #FF8E53 100%);
  color: #ffffff;
  border-radius: 25rpx;
  height: 60rpx;
  padding: 0 40rpx;
  font-size: 26rpx;
  font-weight: 500;
  display: flex;
  align-items: center;
  justify-content: center;
  border: none;
  box-shadow: 0 4rpx 15rpx rgba(255, 107, 139, 0.3);
  transition: all 0.3s ease;
  min-width: 180rpx;
  margin: 0;
  padding-left: 0;
  flex-shrink: 0;
  white-space: nowrap;
}

.login-btn-prompt:active {
  transform: scale(0.95);
  box-shadow: 0 2rpx 10rpx rgba(255, 107, 139, 0.3);
}

.login-btn-prompt .btn-text {
  margin-right: 6rpx;
}

.login-btn-prompt .btn-arrow {
  font-size: 22rpx;
  font-weight: bold;
}

.login-btn-prompt::after {
  border: none;
}

.login-btn-prompt .btn-arrow {
  font-size: 24rpx;
  font-weight: bold;
}

.login-btn-prompt::after {
  border: none;
}

.edit-btn {
  background-color: #f86b5f;
  color: #ffffff;
  border-radius: 8rpx;
  padding: 8rpx 16rpx;
  font-size: 24rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  border: none;
  box-shadow: 0 2rpx 8rpx rgba(248, 107, 95, 0.3);
  box-sizing: border-box;
  position: relative;
  overflow: hidden;
  min-width: auto !important;
  flex-shrink: 0;
  width: 80rpx !important;
  height: 48rpx;
  line-height: 48rpx;
}

.edit-btn::before {
  content: '';
  display: block;
  width: 0;
  height: 0;
}

.edit-btn:active {
  background-color: #e65a4e;
  transform: scale(0.95);
}

.edit-btn::after {
  border: none;
}

/* 未登录时的空白区域，保持布局对齐 */
.empty-space {
  width: 120rpx;
  height: 60rpx;
}

/* 功能菜单 */
.menu-section {
  background-color: #ffffff;
  border-radius: 16rpx;
  margin-bottom: 20rpx;
  box-shadow: 0 4rpx 12rpx rgba(0, 0, 0, 0.05);
}

.menu-item {
  display: flex;
  align-items: center;
  padding: 30rpx;
  border-bottom: 2rpx solid #f5f5f5;
}

.menu-item:last-child {
  border-bottom: none;
}

.menu-icon {
  width: 50rpx;
  height: 50rpx;
  margin-right: 25rpx;
  display: flex;
  align-items: center;
  justify-content: center;
}

.menu-icon image {
  width: 40rpx;
  height: 40rpx;
}

.menu-text {
  flex: 1;
  font-size: 30rpx;
  color: #333333;
}

.menu-arrow {
  width: 30rpx;
  height: 30rpx;
  opacity: 0.5;
}

/* 退出登录按钮 */
.logout-btn {
  width: 100%;
  height: 90rpx;
  line-height: 90rpx;
  padding: 0;
  font-size: 32rpx;
  border-radius: 16rpx;
  background-color: #ffffff;
  color: #f86b5f;
  border: 2rpx solid #f86b5f;
  margin-top: 20rpx;
}

.logout-btn::after {
  border: none;
}